export interface AtletaPelotao {
  id: number;
  nome: string;
  selecionado: boolean;
}

export interface DadosCamiseta {
  nome: string;
  temInstagram: boolean;
  tamanho: string;
  numero: string;
}
